"Setuptools configuration"

# stdlib
from setuptools import setup

if __name__ == '__main__':
    setup(
        name='ncfacbot',
        version='1.0.0',
        author='haliphax',
        packages=['ncfacbot'],
    )
