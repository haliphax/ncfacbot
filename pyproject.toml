[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "ncfacbot"
requires-python = ">=3.11"
version = "1.0.0"

[tool.setuptools]
packages = ["ncfacbot"]
